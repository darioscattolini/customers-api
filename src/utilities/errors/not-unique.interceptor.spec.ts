import { NotUniqueInterceptor } from './not-unique.interceptor';

describe('NotUniqueInterceptor', () => {
  it('should be defined', () => {
    expect(new NotUniqueInterceptor()).toBeDefined();
  });
});
